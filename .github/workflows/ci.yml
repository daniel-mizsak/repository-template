---
name: CI

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "0 12 * * 6" # Every Saturday at 12:00

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  prek:
    permissions:
      contents: read
    uses: daniel-mizsak/workflows/.github/workflows/prek.yml@743ecbc57c37b3daeaa27a2897b2bdf524456125 # v2.3.2

  lint:
    permissions:
      contents: read
      pull-requests: write
    uses: daniel-mizsak/workflows/.github/workflows/lint.yml@743ecbc57c37b3daeaa27a2897b2bdf524456125 # v2.3.2
    with:
      megalinter-config: "./.github/linters/.megalinter.yml"
